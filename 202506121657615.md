# Cursor Talk to Figma MCP 项目结构说明文档

## 📋 项目概述

**项目名称**: cursor-talk-to-figma-mcp  
**版本**: 0.2.1  
**描述**: 实现Cursor AI和Figma之间的Model Context Protocol (MCP)集成，允许Cursor通过编程方式读取和修改Figma设计  
**许可证**: MIT License  
**作者**: sonnylazuardi  

## 🏗️ 项目架构

本项目采用三层架构设计：

1. **MCP服务器层** - 提供标准化的AI助手接口
2. **WebSocket通信层** - 负责实时双向通信
3. **Figma插件层** - 在Figma环境中执行具体操作

## 📁 目录结构详解

```
cursor-talk-to-figma-mcp/
├── src/                          # 源代码目录
│   ├── talk_to_figma_mcp/        # MCP服务器核心模块
│   │   ├── server.ts            # MCP服务器主文件 (91KB, 3032行)
│   │   ├── package.json         # MCP服务器依赖配置
│   │   ├── tsconfig.json        # TypeScript配置
│   │   └── bun.lock            # 依赖锁定文件
│   ├── cursor_mcp_plugin/        # Figma插件模块
│   │   ├── manifest.json        # Figma插件配置清单
│   │   ├── code.js              # 插件核心逻辑 (114KB, 3916行)
│   │   ├── ui.html              # 插件用户界面 (21KB, 661行)
│   │   └── setcharacters.js     # 字符设置工具 (6.5KB, 216行)
│   └── socket.ts                # WebSocket服务器 (5.2KB, 177行)
├── scripts/                      # 构建和部署脚本
│   └── setup.sh                # 项目初始化脚本
├── package.json                 # 项目主配置文件
├── tsconfig.json               # TypeScript全局配置
├── tsup.config.ts              # 构建工具配置
├── smithery.yaml               # Smithery配置文件
├── Dockerfile                  # Docker容器化配置
├── readme.md                   # 项目说明文档
├── .gitignore                  # Git忽略规则
├── LICENSE                     # MIT开源许可证
└── bun.lock                    # 依赖锁定文件
```

## 🎯 核心组件功能

### 1. MCP服务器 (`src/talk_to_figma_mcp/server.ts`)

**功能概述**: 实现Model Context Protocol标准，为AI助手提供与Figma交互的能力

**主要功能模块**:

#### 📄 文档与选择操作
- `get_document_info` - 获取当前Figma文档信息
- `get_selection` - 获取当前选中元素信息
- `read_my_design` - 获取选中元素的详细节点信息
- `get_node_info` - 获取指定节点的详细信息
- `get_nodes_info` - 批量获取多个节点信息

#### 📝 注释管理
- `get_annotations` - 获取文档或节点的所有注释
- `set_annotation` - 创建或更新注释（支持Markdown）
- `set_multiple_annotations` - 批量创建/更新注释
- `scan_nodes_by_types` - 按类型扫描节点（用于查找注释目标）

#### 🔗 原型和连接
- `get_reactions` - 获取所有原型反应（带视觉高亮动画）
- `set_default_connector` - 设置默认连接器样式
- `create_connections` - 在节点间创建FigJam连接线

#### ✨ 元素创建
- `create_rectangle` - 创建矩形元素
- `create_frame` - 创建框架容器
- `create_text` - 创建文本节点

#### 📝 文本内容管理
- `scan_text_nodes` - 智能扫描文本节点
- `set_text_content` - 设置单个文本内容
- `set_multiple_text_contents` - 批量更新文本内容

#### 📐 自动布局和间距
- `set_layout_mode` - 设置布局模式（NONE/HORIZONTAL/VERTICAL）
- `set_padding` - 设置内边距
- `set_axis_align` - 设置轴对齐方式
- `set_layout_sizing` - 设置尺寸模式
- `set_item_spacing` - 设置子元素间距

#### 🎨 样式设置
- `set_fill_color` - 设置填充颜色（RGBA）
- `set_stroke_color` - 设置描边颜色和粗细
- `set_corner_radius` - 设置圆角半径

#### 📏 布局和组织
- `move_node` - 移动节点位置
- `resize_node` - 调整节点尺寸
- `delete_node` - 删除单个节点
- `delete_multiple_nodes` - 批量删除节点
- `clone_node` - 克隆节点

#### 🧩 组件和样式
- `get_styles` - 获取本地样式信息
- `get_local_components` - 获取本地组件信息
- `create_component_instance` - 创建组件实例
- `get_instance_overrides` - 提取组件实例覆盖属性
- `set_instance_overrides` - 应用覆盖属性

#### 📤 导出功能
- `export_node_as_image` - 导出节点为图片（PNG/JPG/SVG/PDF）

#### 🔌 连接管理
- `join_channel` - 加入指定频道与Figma通信

### 2. WebSocket服务器 (`src/socket.ts`)

**功能概述**: 提供实时双向通信桥梁，连接MCP服务器和Figma插件

**核心特性**:
- 基于WebSocket协议的实时通信
- 频道管理机制
- 消息路由和转发
- 跨平台支持（包括Windows WSL）
- 默认端口: 3055

**通信流程**:
```
MCP Server ←→ WebSocket Server ←→ Figma Plugin
```

### 3. Figma插件 (`src/cursor_mcp_plugin/`)

**功能概述**: 在Figma环境中执行具体的设计操作

**核心文件**:

#### `manifest.json`
- 插件配置清单
- 支持Figma和FigJam编辑器
- 网络访问权限配置
- 动态页面访问权限

#### `code.js`
- 插件核心逻辑实现
- Figma API调用封装
- 与WebSocket服务器的通信接口
- 设计元素的CRUD操作

#### `ui.html`
- 插件用户界面
- 频道连接管理
- 实时状态显示
- 操作反馈界面

#### `setcharacters.js`
- 字符和文本处理工具
- 文本样式设置
- 字符编码处理

## 🛠️ 技术栈

### 运行时环境
- **Bun**: 现代JavaScript运行时和包管理器
- **Node.js**: 兼容的JavaScript运行环境

### 核心依赖
- **@modelcontextprotocol/sdk**: MCP协议标准实现
- **ws**: WebSocket通信库
- **uuid**: 唯一标识符生成
- **zod**: 数据验证和类型安全

### 开发工具
- **TypeScript**: 类型安全的JavaScript超集
- **tsup**: 现代TypeScript构建工具
- **Docker**: 容器化部署支持

## 🚀 构建与部署

### 构建脚本
```json
{
  "start": "bun run dist/server.js",        // 启动MCP服务器
  "socket": "bun run src/socket.ts",        // 启动WebSocket服务器
  "setup": "./scripts/setup.sh",           // 项目初始化
  "build": "tsup",                         // 构建项目
  "build:watch": "tsup --watch",           // 监听模式构建
  "dev": "bun run build:watch",            // 开发模式
  "pub:release": "bun run build && npm publish"  // 发布版本
}
```

### 部署方式

#### 本地开发
1. 安装Bun运行时
2. 执行`bun setup`初始化项目
3. 启动WebSocket服务器: `bun socket`
4. 启动MCP服务器: `bunx cursor-talk-to-figma-mcp`
5. 在Figma中安装并运行插件

#### NPM包发布
- 包名: `cursor-talk-to-figma-mcp`
- 二进制命令: `cursor-talk-to-figma-mcp`
- 支持全局安装和直接运行

#### Docker容器化
- 提供Dockerfile支持
- 支持容器化部署
- 便于CI/CD集成

## 🔧 配置管理

### Cursor MCP集成配置
```json
{
  "mcpServers": {
    "TalkToFigma": {
      "command": "bunx",
      "args": ["cursor-talk-to-figma-mcp@latest"]
    }
  }
}
```

### WebSocket连接配置
- 默认地址: `localhost:3055`
- 支持跨域访问
- Windows WSL支持: 设置hostname为`0.0.0.0`

### Figma插件权限
- 动态页面访问权限
- 网络访问权限
- 开发环境本地连接支持

## 📊 项目特色功能

### 1. 智能文本替换
- 支持批量文本内容替换
- 智能文本节点扫描
- 大型设计文件的分块处理

### 2. 实例覆盖传播
- 组件实例属性提取
- 批量覆盖属性应用
- 减少重复设计工作

### 3. 注释转换
- 手动注释转Figma原生注释
- Markdown格式支持
- 批量注释管理

### 4. 原型转连接线
- 原型反应提取
- 自动生成FigJam连接线
- 可视化流程图创建

## 🎯 AI助手提示策略

项目内置多个AI助手指导策略：

- `design_strategy` - Figma设计最佳实践
- `read_design_strategy` - 设计读取策略
- `text_replacement_strategy` - 文本替换系统方法
- `annotation_conversion_strategy` - 注释转换策略
- `swap_overrides_instances` - 实例覆盖传输策略
- `reaction_to_connector_strategy` - 原型转连接线策略

## 🌟 使用场景

### 设计自动化
- 批量内容更新
- 样式统一调整
- 布局自动优化

### 设计文档化
- 自动注释生成
- 设计规范提取
- 组件文档化

### 原型优化
- 交互流程可视化
- 原型连接线生成
- 用户流程图创建

### 协作增强
- AI辅助设计决策
- 智能设计建议
- 自动化重复任务

## 📝 版本管理

- 当前版本: 0.2.1
- 版本控制: Git
- 发布渠道: NPM Registry
- 社区支持: GitHub Issues

## 🤝 贡献指南

项目采用开源协作模式，欢迎社区贡献：

- 功能增强
- Bug修复
- 文档完善
- 使用案例分享

主要贡献者包括 [@dusskapark](https://github.com/dusskapark) 等社区开发者。

---

*本文档生成于: $(date +"%Y-%m-%d %H:%M:%S")*  
*项目地址: https://github.com/sonnylazuardi/cursor-talk-to-figma-mcp* 